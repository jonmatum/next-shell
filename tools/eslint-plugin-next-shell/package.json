{
  "name": "eslint-plugin-next-shell",
  "version": "0.0.0",
  "private": true,
  "description": "Internal ESLint rules for @jonmatum/next-shell — enforces the semantic-token-only policy.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.js",
  "exports": {
    ".": "./src/index.js"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "echo \"(no tests yet)\" && exit 0",
    "build": "echo \"(pure JS, no build step)\" && exit 0"
  },
  "peerDependencies": {
    "eslint": "^9.0.0"
  }
}
